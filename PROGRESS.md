# PROGRESS — ESM 進捗ログ

各フェーズ/作業で「何をやったか・なぜそう決めたか・次は何か」を記録する。**新しい作業ほど上に追記**。日付は絶対表記（例: 2026-06-20）。プロジェクト全体の指針は `CLAUDE.md` 参照。

凡例：✅ 完了 / 🚧 進行中 / ⏭ 次の予定

---

## 全体を書き出す（外部AIに自分の文脈を渡す）　✅（2026-09-20）

**要望**：ESMの中にある自分の情報を ChatGPT/Claude に渡したい。既存の「全設問コピー」は1社分だけなので、**全体**を渡したい。

**途中で出た問い — 「APIのような形で繋げられないのか」**
繋げられる。選択肢は3つあった。
- **A. MCPコネクタ**：`functions/` に `mcp` 関数を足し、Firestore を読む read-only ツールを ChatGPT/Claude のコネクタとして公開する。両対応で本命だが、**壁は認証**。既存の Firebase IDトークンはブラウザのログイン由来で有効期限1時間、外部AIは更新できない。正攻法は OAuth サーバー（認可・トークン・DCR・PKCE・リフレッシュトークン保存）の自前運用＝ESM本体より重い新サブシステム。妥協案の「トークン入り秘密URL」は、URLが漏れた瞬間に全データが読める。
- **B. ChatGPT の Custom GPT + Actions**：OpenAPI＋APIキーで軽いが **ChatGPT専用**。「ChatGPTやClaude等に」という要望と噛み合わない。
- **C. Markdown 書き出し**：実装ほぼゼロ。毎回貼る手間が残る。

**→ C を採用。決め手は「欲しいのは常時同期ではない」こと。**
動機は「使い慣れたチャットで相談したい」であって、リアルタイム同期ではなかった。ChatGPT にも Claude にも **プロジェクト**があり、そこに `.md` を1つ置けばプロジェクト内の全会話が参照する＝**貼り直しは発生しない**。更新は月に数回の差し替えで足りる。Aが効いてくるのは「差し替えが苦痛になるほど更新が速いとき」だけで、そのために OAuth を建てるのは設計原則4（余計な機能を作らない）に反する。
なお **Cの作業はAに進んでも無駄にならない**：整形ロジック（データ → 読みやすいMarkdown）は MCPツールが返す中身とほぼ同じものなので、そのまま持って行ける。

**やったこと**
- アカウントメニューに `📤 全体を書き出す` を追加（`Claudeへの指示` と `？ 使い方` の間）。`exportAll()` 58行、Blob + `a[download]` で `esm-YYYY-MM-DD.md` を保存。新しい依存なし、`callAI()` にも Functions にも触っていない。
- 構成：素材庫（`MAT_CATS` の順・空カテゴリは飛ばす）→ 企業（年度タブが出ているときだけ `## 企業（27卒）` で分割、サイドバーと同じ `compareCompanies()` 順）→ 企業メタ（締切・提出済み・選考結果／落選段階＋メモ・未完了の作業）→ 設問と草案。

**草案の扱い（作業中の追加要望で方針転換）**
当初は全設問コピーと同じく「完了チェック済みの草案を1つだけ」出す設計だったが、**没にした案も相談の材料になる**（「なぜその書き方をやめたか」が分かる）ため、**全草案を出す**ことにした。あわせて：
- **`d.memo`（UIでは note）も出す**。引用ブロックにして本文と混ざらないようにした。
- **実際に提出した版が見分けられるようにした**。完了チェック済み(`d.done`)の草案に印を付け、企業が `c.submitted` なら `★ 実際に提出した版`、未提出なら `★ 確定稿`。この2つは意味が違う（前者は事実、後者は本人の確定操作にすぎない）ので、勝手に同一視せず出し分けた。
- 中身もメモも空の草案は落とす（ノイズになるだけ）。
- 既存の「全設問コピー」ボタンは**触っていない**（1社分を素早く貼る用途なので、全草案を出すと逆に使いにくい）。

**意図的に含めないもの**
- **マイページの `url` と `loginId`** — ログイン情報を外部AIに渡す理由がない。ここが今回いちばん気をつけた点。
- **AI相談の会話履歴 `chat`** — 草案という結論があれば足りるし、量が膨らんで本体を薄める。使用量 `aiUsage` も同様。

**検証**：サンプルデータを Node に流して出力を確認（loginId/URL が出力に含まれないことを正規表現で確認／落選が `1次落ち（メモ）` になる／年度で見出しが分かれる／done草案が優先される／空の素材が飛ぶ）。

⏭ **次**：差し替えが面倒になったら A（MCPコネクタ）を検討する。そのときは ChatGPT/Claude のコネクタ仕様を最新で裏取りしてから設計を書く。

---

## 英語版ランディングページ＋英語README（海外インターン応募向け）　✅（2026-09-19）

**背景**：海外のインターンに応募するにあたり、ESM を作成物として提出したい。英語版が要る。

**最初に決めたこと — 英訳ではなく、別の読者に向けた別のページにする**
既存 `landing.html` は「就活生に使ってもらう販促ページ」（ヒーロー＝「就活ESを一か所に」、CTA＝「無料で始める」、機能カード14枚）。これをそのまま英訳しても選考官には刺さらない。理由は2つ：
- **選考官はユーザーではない**。知りたいのは「何を作ったか」ではなく「**なぜそう判断したか／何が難しかったか**」。機能カード14枚は販促としては正しいが、評価材料としてはノイズ。
- **"Entry Sheet" と "就活" が通じない**。海外にESという概念はない。「新卒一括採用では数十社に数百〜千字の作文を締切つきで出す」を最初の数秒で通さないと、ページ全体が意味不明になる。逆にそこを説明できれば「知らない文化の問題を構造化して解いた」という読まれ方になる。

→ **ハイブリッド構成**を採用（本人判断）。上半分＝プロダクト、下半分＝エンジニアリング。応募職種はソフトウェアエンジニア。

**やったこと**
- **`landing.en.html`（新規826行）**。構成：Hero → Proof（実測値4つ）→ The problem → What it does（6枚）→ Architecture（図）→ **Engineering decisions** → Security model → **作らなかった機能** → CTA。
  - 設計判断は各カードを `Why` / `Rejected` の2ブロックに分け、色も分けた（青＝採用、琥珀＝却下）。**却下理由が視覚的に同じ重さで並ぶ**ので、飛ばし読みされても「捨てた案を書いている」が伝わる。
  - Security セクションに「Firebase `apiKey` は秘密ではない」を独立項目で置いた。fail-closed の項では**旧実装が空リストで判定をスキップしていたこと、それを直したこと**まで書いた（自分の過去のミスを書けることが一番効く）。
  - トークンは日本語版を継承（`#2563EB`／DM Mono）、本文のみ Inter。**ダークモード**（`prefers-color-scheme`）と `prefers-reduced-motion` に対応。
- **`README.en.md`（新規191行）**。日本語版の直訳ではなく、ランディングページと同じ「Why + Rejected」の骨格に揃えた（ページ→GitHub と読み進めても話が一貫する）。日本語版になかった追記として、**allowlistの2名はAI機能限定で、アプリ本体のアクセス制御とは別物**だと明記（これを書かないと「10人以上に配布」と「2 UIDに限定」が矛盾して見える）。
- 日本語版 `landing.html` にはナビの言語切替リンク（English）を1本追加したのみ。CSSは規約どおり**メディアクエリより前**に挿入。

**数字の扱い — 盛らないことを優先した**
statsは全部リポジトリの実測値（169コミット／2,504行／フロント依存0）。利用者については当初「2 real users — me and my brother」と書いたが、実態は**10人以上に配布済み**との指摘。ただし継続利用は追跡していないため、**「配布した」までしか主張しない**（`10+ job-hunting students it has been shared with`）と決めた。面接で突っ込まれても整合する範囲に留めるため。
→ これに伴い「2人」前提の記述4箇所（決定01の理由、コマンドパレット却下理由、CTA注記、stats）を直した。**allowlistの2 UIDは今も事実なので残した**。

**ついでに直した古い記述**（英語版だけ直すと日本語側と数字が食い違うため）
- `CLAUDE.md` / `README.md`：利用者「2名」→ 10人以上に配布
- 同：行数 約2100〜2200行 → 約2500行（実測2,504）
- `CLAUDE.md`：Firestoreルール「コンソール管理（**リポジトリ未管理**）」→ `firestore.rules` として管理下にある旨。README.md と矛盾していた
- `CLAUDE.md`：アーキテクチャ節に `landing.en.html` を追記
- `CLAUDE.md`：git push のメモ「**実ターミナルから**」→ 実際には classic PAT が `credential.helper=store` で `~/.git-credentials` に保存されており**非対話シェルからも通る**（この日2回成功）。平文保存の注意とPAT再発行手順を併記。

**検証**
- 両HTMLのタグ対応を `html.parser` で検査 → balanced。内部アンカー（`#top` `#engineering`）とidの一致、相互リンクの実在、GitHub URLが `git remote` と一致することを確認。
- `README.en.md` が参照する10パスすべての実在、MIT（LICENSE実物と一致）、`firestore.rules` の引用が実ファイルと一致することを確認。「依存ゼロ」は**フロントに限定した書き方**にしてあるので `functions/` の3依存と矛盾しない。
- 本番反映後、配信バイト数の一致・`10+` の表示・旧文言の消失・`README.en.md` へのリンク出現を curl で確認。

**詰まった点**
- `pkill -f "http.server 8000"` が**その文字列を含む自分自身のコマンドラインにマッチ**してシェルごと落ちた（exit 144）。パターンを `http\.server` に変えて分離実行で解決。
- **`CLAUDE.md` のコミットが auto mode の分類器に `[Self-Modification]` でブロックされる**（commit単独でも、push込みでも）。変更内容が「Claude Code から直接 push してよい」という自分の権限を広げる記述だったため、ガードレールとしては妥当。回避せず、コマンドを本人に渡して実ターミナルから実行してもらった（`8042a1f`）。**`CLAUDE.md` 自体が「アーキテクチャが変わったら必ず更新」と指示している以上これは再発するので、今後は編集→差分提示→本人がコミット、の手順にする。**

**コミット**：`bd82cab`（英語ランディング）／`b142adb`（英語README＋訂正）／`8042a1f`（gitメモ訂正・本人実行）

**⏭ 次**：`PROGRESS.md` と `docs/` の英訳は**不要**と判断（README.en.md に Japanese と明記済みで読み手は迷わないし、選考官がそこまで潜る前に判断材料は出揃っている）。

---

## キーボードだけで動かす（自動フォーカス＋Esc移動モード＋矢印）　✅（2026-09-12）

**要望**：「企業を↑↓、設問を←→で移動したい。あと最初から草案の入力ボックスにカーソルがあっていたら楽」
→ 掘ると本当のゴールは **「マウスを触らずに、該当の企業の設問に行って内容を編集できる」**。

**やったこと**
- **自動フォーカス**：起動時／設問を選んだとき／草案ピルを選んだとき／検索結果から跳んだときに、本文 `#cardText` へフォーカス（`focusBody()`、カーソルは文末）。**≤640px では無効**（ソフトキーボードで画面が潰れるため）。
- **Esc で「移動モード」**：モーダル／ドロワーが開いていればそれを閉じ、何も開いていなければ本文から `blur()`。
- **移動モード中の矢印**：`↑` `↓` で企業（`coNavState()`／サイドバー表示順 `compareCompanies` そのまま）、`←` `→` で設問（既存 `qNavState()` を再利用）。端で止まり**ループしない**。`preventDefault()` でページスクロールを止める。
- **`Enter` で本文へ復帰**。`revealActive()` で移動先が画面外なら寄せる。
- 移動モード中は選択中の企業／設問に枠（`body.nav-mode`）。ヘルプに「⌨️ キーボードだけで動かす」を追加。

**なぜこの形か**
- **2つの要望は正面からぶつかる**。旧ハンドラ（`:2135`）は `INPUT/TEXTAREA/contenteditable` で早期 return するため、本文に自動フォーカスすると**素の矢印は一生発火しない**。モードで分けるしかなかった。
- **復帰キーは素の `Enter`**。移動モードではフォーカスが `<body>` にあり Enter が完全に空いている＝1キーで済む。`Esc` = 出る / `Enter` = 入る、と対になる。
  - `Tab` は不採用：ブラウザ標準のフォーカス送りを奪うと、移動モードから完了ボタンや文字数制限欄へキーボードで行けなくなる。
  - `Ctrl+Enter` は不採用：素の Enter が空いている場所で2キー押すのは損。
- **矢印移動では本文にフォーカスを戻さない**。戻すと次の矢印がカーソル移動に吸われて連続移動できない。
- **`Ctrl+K` 検索ジャンプは却下**（本人判断：「検索では面倒」）。企業名は検索対象外のままなので、必要になったらそこから。

**ついでに直ったこと**
- `Esc` の処理を早期 return より**前**に出したので、**本文やモーダル内の入力欄を触っている最中でも Esc でモーダルが閉じる**ようになった（旧実装では閉じなかった）。
- 修飾キー付きのキーを奪わないようにした（旧実装は `Ctrl+N` で設問が増えていた）。

**検証**
- jsdom で firebase をスタブして実キー入力を再現し、**デスクトップ24件＋モバイル6件を全PASS**（自動フォーカス、Esc、端で止まる、ループしない、preventDefault、矢印では戻らない、カーソル文末、Ctrl併用は奪わない、入力中のEscでモーダルが閉じる、モバイルでは自動フォーカスしない 等）。テストはスクラッチパッドのみでリポジトリには入れていない（ビルド工程なし方針のため）。

---

## 設問の前後移動（設問番号＋矢印）　✅（2026-09-01）

**やったこと**
- 設問カードのヘッダーのラベルを「設問文」→「**設問 2 / 5**」（現在地＋総数）に置換。
- ツールバー（完了ボタンの隣）に `←` `→` を追加。`qIndex()` / `qNavState()` / `gotoQuestion()`。
  - 端では `disabled`。**ループしない。** 設問が1つなら矢印ごと出さない。
  - `activeQId` が実在しない場合も安全側（何も出さない）に倒す。
- 「※ 制限なしの場合は空欄」の注記を削除。

**なぜツールバー（下）でヘッダー（上）ではないか**
- ご要望は当初「設問文のボックスに矢印」だったが、**次へ行きたくなるのは読み終えた＝ページ下端にいるとき**。ヘッダーに置くと毎回上までスクロールして戻ることになる。
- ツールバーには既に「完了」があり、「この設問は一区切り」の操作が集まる場所なので意味的にも合う。
- `←`/`→` だけを設問番号の横に置くと、ツリーのドラッグ並べ替えと読み違える余地もあった。

**背景**
- ドロワー化で、同じ企業の設問を順に見るのに毎回「☰ → 設問」の2タップが必要になっていた。設問5個なら10タップ。
- 「一定以上スクロールしたら次の設問へ」（TikTok/Instagram風の引っ張り発火）も検討したが見送り。誤発火しない作りにはできるものの、(1)同じ摩擦を消すのにコード量が10倍、(2)アプリ全体がそのジェスチャで動いているわけではないので発見されず結局ボタンが要る、(3)順送りしかできずドロワーは残る、の3点から。ボタンを使ってみて物足りなければ、上に重ねる形で後から追加できる。

**検証**
- 前後移動を12件確認（位置、端の無効化、1問のみ、設問ゼロ、`activeQId` 不整合、端で押しても壊れない）。既存37件も回帰なし。

---

## モバイルレイアウト対応　✅（2026-09-01）

設計は `docs/superpowers/specs/2026-09-01-mobile-layout-design.md`。用途は**確認中心＋軽い手直し**と定義（執筆とAI相談はPC前提）。

**やったこと**
- **サイドバー → ドロワー**（≤640px）。`position:fixed` の左オーバーレイ＋バックドロップ。状態は `body.drawer-open` クラス1つ、JSは `setDrawer()` など小さな関数のみ。
  - **企業を選んでも閉じない／設問を選んだら閉じる。** 企業選択＝設問の展開なので、続けて設問を選ばせるため。検索結果から設問へ跳んだときと設問追加時も閉じる。
  - モバイルでは起動時に開く（＝開いたら企業一覧が見える）。
- **ヘッダーを1行に維持。** ロゴを `ESM` に短縮し、ズームと使用量チップを非表示、同期はドットのみ。合計約202px＋検索173pxで375pxに収まる。
- **iOS の自動ズーム対策。** 16px未満の入力欄にフォーカスすると iOS Safari が拡大して戻らない。該当する6種を16pxに。本文は `max(16px, calc(14px * var(--textarea-zoom,1)))` としてPCのズーム設定を持ち込んでも下回らせない。
- **`dvh` 併記。** URLバーの分だけ `100vh` が可視領域より大きく下端が切れる。`#appScreen` と各モーダルに `height:100vh; height:100dvh;` の順で。
- **hover依存の実バグ2件を修正**（`@media(hover:none)`）。`.btn-copy`（本文コピー）と `.ctree-close`（企業の×）は `opacity:0`＋`:hover` だったため、**タッチ端末では表示されず＝スマホから企業を削除できなかった**。×は誤タップを避けるため選択中の企業だけに出す。
- 本文の余白を削って実効幅 283px → 331px。モーダルは全画面化。作業行は2段に折り返し。

**詰まり＆対処（再発時の参考）**
- **メディアクエリは詳細度を上げない。** 最初 `<style>` の中ほどに書いたため、後方で定義される `.material-modal{height:80dvh}` や `#ivModal` を上書きできなかった。**レスポンシブのブロックは `<style>` の末尾に置く**（ファイル冒頭にその旨のコメントを残した）。
- `#ivModal` などは **id指定なので `.modal`(クラス)より強い**。`height`/`max-width`/`max-height` をid側でも指定し直す必要があった。
- `style="max-width:360px"` を**インラインで持つモーダルが2つ**あり、ここだけは `!important` でしか上書きできない。

**検証**
- ドロワーの開閉ルールをDOMシム＋Nodeで12件確認（境界640px/641px、企業選択では閉じない、設問選択で閉じる、PCでは触らない）。既存の並び順11件・company-bar 14件も回帰なし。
- CSSの波括弧の対応とJS構文を機械的に確認。
- ⚠️ **未実施：実機確認。** iOSの自動ズームとURLバーによる `100vh` の挙動はエミュレータで再現しきれないため、実機での確認が必要。

**決定事項**
- ブレークポイントは 640px 1本のみ。`@media(hover:none)` だけ入力方式の軸として併存。
- モバイル起動時は**常に**ドロワーを開く。当初specは「企業未選択のときだけ」としていたが、`activeCompanyId` は復元されるので2回目以降は成立せず機能が死ぬため変更した。
- ズームと使用量チップはモバイルで隠す。同期ドットは残す（設計原則2）。
- モーダルは全画面。ボトムシートは不採用（キーボード表示時に不利）。
- スマホでの執筆最適化は非目標。

---

## 期限切れを最下部へ＋落選段階の記録　✅（2026-09-01）

**やったこと**
- サイドバーの並びを4グループに変更。`sortRank()` と `compareCompanies()` を `renderCompanyTree()` から関数に切り出した。
  - `0`=期限内にやることあり（期限が近い順）→ `1`=やることなし（提出済・結果待ち）→ `2`=落選 → `3`=**期限切れ**（直近に切れた順＝古いものほど下）。
  - 落選を先に判定するので、落選企業に期限切れの作業が残っていても二重には沈まない。
  - 期限切れの行は `.ctree-co.overdue` で薄く表示。
- 落選したときだけ company-bar に「落ちた段階」（ES／Webテスト／1次／2次／最終・押し直しで解除）と1行メモを表示。
  - データは `c.failStage` / `c.failMemo`。未設定なら書き込まない（`c.year` と同じ方針）。
  - `setResult()` を追加し、落選以外に変えた／提出済みを外したときに両方 `delete` する。
  - サイドバーのバッジが `ES落ち`／`1次落ち` などになる。**「1次落ち」＝ESは通過していた**が一覧で読み取れる。
- スマホ：段階ボタンは `flex-wrap` で折り返し、640px以下でタップ領域 `min-height:30px`、メモ入力は `font-size:16px`（16px未満だと iOS Safari が入力時に勝手にズームするため）。

**背景**
- 期限切れは日付が最も古いので、旧ソートでは最上部に居座って一番目立っていた。
- 旧 `c.result` は pass/pending/fail の3値だけで、落選しても**どの段階で落ちたかが残らなかった**。

**検証**
- テスト環境が無いので、`index.html` から対象関数を抜き出して Node で実行する使い捨てスクリプトで確認（並び順11件／company-bar 14件）。
  - company-bar は最小のDOMシムを噛ませて `renderCompanyBar()` を実際に走らせ、クリック→データ更新まで確認。
  - メモは `value=` に埋めず描画後に代入しているので、`"` を含んでもHTMLが壊れないことを確認済み。

**決定事項**
- 期限切れは**落選よりさらに下**（最下部）。
- メモは落選時のみ。通過・結果待ちには付けない（設計原則4：余計な機能を作らない）。
- アプリ全体のモバイル再設計（サイドバー180px固定など）は今回の範囲外。別タスク。

---

## 年度タブ（27卒/28卒）　✅（2026-08-28）

**背景**：作者本人が27卒で就活し、28卒でもう一度やる想定。同じアカウントに2年分の企業が積み上がるため、サイドバーで年度を分けたい。一方 弟（実ユーザー）は1年分しか使わないので、**年度を使わない人の見え方とデータを一切変えない**ことを要件にした。

**設計（なぜこうしたか）**
- 年度は**サイドバーの表示フィルタ**であって空間の切り替えではない。2周目に一番価値があるのは去年書いた文章なので、**検索 `doSearch()` は年度を横断**（無改造）。結果クリック時だけ `activeYear` を合わせて飛ぶ。素材庫・ガイドライン・使用量も年度を持たない。
- **タブが出る条件は `usedYears().length >= 2`**。年度を持つ企業が0社ならタブなし・全件表示＝現行と完全に同一。1年度だけでも出さない（1グループをタブで分ける意味がない）。
- **年度未設定の企業は先頭年度（＝27卒）に属するものとして扱う**（`coYear()`）。データは書き換えないので「どのタブにも出ない企業」は生まれない。当初は「2年度目を付けた瞬間に確認ダイアログを出して一括で年度を書き込む」設計だったが、ダイアログを断られると不可視の企業ができるうえ、そもそも書き込む必要がなかったので廃止した。
- 割り当てUIは**企業追加/編集モーダルの `年度` セレクト1つだけ**（`指定なし / 27卒 / 28卒`）。設定トグルも年度追加ボタンも作らない。追加時の既定値は表示中のタブ（`activeYear`）なので、普段は触らなくていい。
- `saveToCloud()` は `activeYear` が null なら**キーごと送らない**ので、年度を使わない人の Firestore ドキュメントは現行とバイト単位で同一。

**実装**：`index.html` のみ、+72/-11。`YEARS/usedYears/yearTabsOn/coYear/visibleCompanies/normalizeYear/renderYearTabs` を追加し、`renderCompanyTree()` の並べ替え元を `companies` → `visibleCompanies()` に差し替え。`sortRank()` / `nextDue()` は無改造。

**確認**：年度ヘルパをNodeに切り出して7ケース（タブOFF/1年度/2年度/未設定の帰属/タブ切替時の `activeCompanyId` 付け替え）を通し、Firebaseをスタブしたコピーを headless Chromium で描画して 27卒タブ・年度なし・ライトテーマの3面をスクリーンショット確認。

**ついでに直したもの**：非アクティブなタブの色を `--text3` → `--text2`（実効コントラスト 2.61→5.42 dark / 5.39 light）。クリックできる要素なので 2026-08-21 の基準に合わせた。

---

## 企業ツリー2行目の調整（視認性＋ES提出を非表示）　✅（2026-08-21）

- 2行目が薄すぎて読めなかったため色とサイズを変更。実効コントラスト（非選択行 `opacity:.55` 込み）は **1.58→5.26（dark）/ 1.54→3.66（light）**。完了チェックは 13→15px、枠を `--text` に、**✓ を常時表示**（従来は `color:transparent` でホバー時のみ＝スマホでは永久に見えなかった）。
- **2行目は作業（`kind==='task'`）のときだけ出す**ように変更。ES提出は行そのものが表しているため名前を出さず、残り日数バッジのみで示す。ES提出は `nextDue()` には残してあるので**ソートとバッジは従来どおりES提出期限で動く**（外すとこの機能の核が壊れる）。提出操作はサイドバー下部の「提出済み」ボタンのまま。
- 残課題：ライトテーマは 3.66:1 が上限で小さい文字の推奨 4.5:1 に届かない。原因は色ではなく非選択行の `opacity:.55`。上げるなら「未完了作業を持つ行だけ薄くしない」形になる。

---

## 企業ツリーの操作性改善（完了チェック／〇〇削除／設問の開閉）　✅（2026-08-21）

- 企業行の2行目（次にやること）に**完了チェック**を追加。閉じている企業でも1クリックで消せる。完了すると `nextDue()` が次の作業を返すので2行目が自動で切り替わり、全部終わると2行目が消えて1行に戻る。
- ES提出の仮想行のチェックは `c.submitted=true`（企業操作バーの「提出済み」と同じ）。判別のため `nextDue()` の戻り値に `kind`（`'es'`/`'task'`）と `id` を追加。
- **達成度バッジ 〇/〇（`.ctree-prog`）を廃止**。※削除ブロック内で宣言していた `const qs=c.questions||[]` は下の設問展開が使うため残す（消すと ReferenceError）。
- **設問ツリーを開閉可能に**。`c.collapsed` を追加し、展開条件を `isActive && !c.collapsed` に。**選択中の企業の行をクリック**でトグル（今まで何も起きなかった操作に割り当て、新しいタップ対象を増やさない）。キャレット ▶ の回転も同条件。企業名は contentEditable で `stopPropagation` 済みなので名前クリックでは開閉しない。
- 検証：最小DOMスタブに実物の `renderCompanyTree()` を載せ、完了→次の作業へ切替→2行目消滅、開閉、ES提出チェックまで15項目を Node で実行して確認。

---

## 期限つき作業（due tasks）＋選考順ソート　✅（2026-08-21）

- 企業に **`tasks[]`（期限つき作業 `{id,name,due,done}`）** を追加。ウェブテスト・適性検査・動画提出のような「固定日時ではないが期限がある作業」の置き場所を作った。追加・編集は既存の企業モーダル（提出期限の下）。
- **`nextDue(c)`** を新設＝その企業の「次にやること」を1件返す純粋関数。未完了タスクに加え、ES提出期限（`c.deadline`）を**データを作らず仮想の1件として合成**する。ツリーの並び順・バッジ・2行目はすべてここから導出。
- **並び順の前提を修正**：旧 `sortRank` は「ES提出＝一段落」と決め打ちして提出済みを沈めていたが、実際は次フェーズの開始。新ランクは 次にやることがある(期限昇順) → やることなし → 落選。**一番動きのある企業が一番上に来る**ようになった。
- 企業行を2行化し、下段に作業名を表示（`.ctree-co-sub`）。`submitted` クラスの付与条件を `c.submitted && !nextDue(c)` に変更＝作業が残っている企業を薄くしない。
- **企業行のドラッグ並べ替えを削除**。並びが状態から自動導出されるようになり、掴めるのに動かない操作になったため。設問のドラッグは維持。
- 併せて既存バグを修正：企業編集を Escape で閉じると `dataset.editId` が残り、次の新規追加が**直前に編集していた企業を空フォームで上書き**していた。
- 非目標として記録：面接など固定日時の予定・通知・カレンダービュー・ウェブテスト専用フィールド。サイドバー上部の横断リストは一度設計に入れたが、ツリーと情報が重複するため不採用。
- 設計: `docs/superpowers/specs/2026-08-21-due-tasks-design.md` / 計画: `docs/superpowers/plans/2026-08-21-due-tasks.md`

---

## 自分史インタビュー（対話で素材を作る・最小版）　✅（2026-06-22）

- 素材庫に **「💬 対話で自分史を作る」**。素材庫が空のとき（初回）は中央にそっと誘導（強制しない・スキップ可）。
- 専用インタビューモーダル（チャットUIを流用）：Claudeが1問ずつ質問→本人が答える→深掘り。会話は `ivChat` で保持。
- **「素材庫に保存」→ 会話から自分史を一人称でまとめ、`materials` に {cat:'自分史'} として保存**。接地（事実は本人、AIは引き出して整理＝捏造しない）。保存後は素材庫に自分史が表示。
- 狙い：王道「①素材を貯める」をAIが直接支援＝素材庫が全機能の土台になりブートストラップ（空だと下書き/添削/切り口/チャットが接地できないため）。
- 最小版は**自分史のみ**。次に同じ仕組みで「ガクチカ版」を足せる。

---

## UI整理 — 切り口提案／複製削除／ヘッダ集約／指示書化　✅（2026-06-21）

- **Phase 2②は「3案フル生成」を廃止し「切り口提案（軽量版）」に**：草案バーの「切り口」ボタン→Claudeが書く方向性を3つチャットに提案→気に入ったものを会話で育てる。草案を散らかさない／"AIに丸投げで量産"を避ける（チャット＝伴走を主役に）。
- **複製ボタンを削除**（草案バー＝＋追加 / Clawd / 切り口）。
- **ヘッダをアカウント(アバター)メニューに集約**：📚素材庫 ／ 📝Claudeへの指示 ／ 使い方 をドロップダウンに移し、独立ボタンを撤去。ヘッダは「同期・使用量・アバター」だけに。
- **「Claudeに教える」→「📝 Claudeへの指示」**（指示書としてプレーンに簡素化、ひな形ボタン廃止）。`guidelines` を全AI機能に注入する仕組みは不変。
- 設計意図：2人運用＝シンプル/クール優先。説明書きを削ぎ、ボタンを集約。

---

## Phase 2 拡張 — Claudeガイド／変更点メモ／ブランディング／Clawd／使用量　✅（2026-06-21）

- **🎓 Claudeに教える（育てるガイドライン）**：編集できる方針を Firestore `guidelines` に保持し、`guidelineBlock()` で全AI機能（チャット/添削/下書き/反映）の system 末尾に注入＝使うほど賢くなる（Claude本体のFTは不可なのでこの方式）。ヘッダにボタン＋モーダル＋ひな形挿入。
- **✨ 添削の変更点メモ**：添削は1回の応答で「本文 ===変更点=== 理由」を出させ、本文→草案／変更点→その草案のメモに分割。完了後にメモを自動で開いて理由表示（追加API呼び出し無し＝コストほぼ不変）。
- **Claudeブランディング＆最小化**：ラベルを「Claude」に統一、案内文・例文を削除（2人運用なのでシンプル/クール優先）。
- **Clawd（マスコット）**：`claude_code_mascot.3mf` 内の実物写真を画像解析し、12×8ドットで忠実再現（コーラル #D77655・黒目2・腕の張り出し・脚4本）。相談欄の空状態に表示。
- **使用量チップ**：各呼び出しの usage を累計し公開単価で概算（≈$）をヘッダ表示・クリックで内訳。Firestore `aiUsage` に保存。正確な請求は Anthropic コンソール。

---

## Phase 2 ① — 自分史チャット（対話モード）　✅ 動作確認（2026-06-21）

**目的**：一発生成/添削だけでなく、AIと往復しながら内容を固める。「何を書けばいいか分からない」摩擦を対話で解く。

**やったこと**
- 草案編集カードに、メモと同じアコーディオンで「💬 AIと相談」をインライン増設（1カラム＝PC/スマホ共通。UIはユーザー選択）。
- 会話は草案ごとに `d.chat=[{role,content}]` で保持（Firestore保存・リロードで復元）。
- `sendChat()`：設問・文字数・企業名・素材庫・現草案を文脈に、会話履歴付きで `callAI()`→ストリーミング返答。AIは質問で深掘りしつつ捏造しない方針。
- `applyChat()`：「この会話を草案に反映」で、会話を踏まえた完成稿を生成し**今の草案に書き戻す**（字数考慮）。反映後は ✨ボタン表示も整合。

**設計判断**
- 別画面を作らず既存導線に溶け込ませた（集中・シンプル）。反映は現草案を上書き（チャットは草案に紐づくため。別案保存は ✨添削/複製で）。

**“育てる”フックの仕込み**
- chat の system プロンプト末尾に、将来の「添削ガイドライン」を注入する場所をコメントで明示（教え込みは プロンプト / few-shot / 好み蓄積 でここに繋ぐ）。

⏭ **次の候補**：(a)「添削ガイドライン」を育てる仕組み（注入＋編集UI）／(b) Phase 2 ② 3通り生成／(c) Phase 3 ④企業提案。

---

## Phase 1 — AI添削 & 下書き（草案バーの ✨ ボタン）　✅ 動作確認（2026-06-21）

**目的**：既存の草案編集に AI を溶け込ませる。③添削（推敲）と、空の草案からの下書き（Phase 2 生成への橋渡し）を、1つのボタンで提供。

**やったこと**
- 草案バーに文脈対応の「✨」ボタンを追加（`refineDraft()`）。`callAI()` 経由でストリーミング表示。
  - 草案が**空** → 「✨ 下書き」：素材庫(materials)＋設問＋企業名をもとに**たたき台を今の草案に生成**。素材に無いことは捏造せず ［要補足：…］ で示す方針。
  - 草案に**文章あり** → 「✨ 添削」：推敲結果を**新しい草案として作成**（元は残す＝書き比べ／データ保全）。
- ボタン表示は草案の中身で自動切替（空＝下書き／文章＝添削）。
- 文字数制限があればその字数以内に収めるよう指示。

**設計判断**
- 別UIを増やさず1ボタンに集約（シンプル維持）。生成は必ず素材庫に接地＝"それっぽい嘘ES"を作らない。
- 「生成(空→1)」は本来 Phase 2 だが、空ボックスの摩擦が実在するため最小形で先行導入。

**ユーザーからの次の要望（→ Phase 2 ① へ）**
- **対話形式**で内容を詰めたい（問答しながら完成）。
- 添削/作成の方針を**育てたい** → Claude本体のファインチューニングはAPI非対応。代わりに「添削ガイドラインのプロンプト注入＋お手本(before→after)few-shot＋好みの蓄積」で実現する方針。

⏭ **次：Phase 2 ①「自分史チャット（対話モード）」** — 設問・素材庫・現草案を文脈に、AIと往復して内容を固め、成果を草案に反映。最初から「ガイドライン注入」フックを仕込み、"育てられる"状態にする。

---

## Phase 0 — Claude APIプロキシ疎通　✅ 完了（2026-06-20）

**目的**：Claude APIキーを安全に隠したまま、フロント（GitHub Pages）からClaudeを呼べる土台を作る。設計の核＝「秘密キーを静的フロントに置かず、サーバ側の薄いプロキシに隠す」。

**やったこと**
- Firebase CLI 導入、`firebase login`、Blazeプランへ変更、Anthropicクレジット $5 購入。
- Cloud Functions（2nd gen / Node20 / asia-northeast1）に薄いプロキシ `ai` を実装・デプロイ。
  - Firebase IDトークン検証 → `ALLOWED_UIDS`（本人＋弟）で認可 → Claude `messages.stream` → SSEで中継。
  - CORSは `koheimukogawa.github.io` ＋ localhost のみ。既定モデル `claude-opus-4-8`、max_tokens上限8000。
- `ANTHROPIC_API_KEY` を Secret Manager に格納（フロント／リポジトリには置かない）。
- `index.html` に全AI機能の共通入口 `callAI()` を追加。
- ブラウザ実機でフル疎通確認（ログイン → プロキシ → Claudeの応答がストリーミング表示）。
- Artifact Registry の自動クリーンアップポリシー設定（保管料対策、1日より古いイメージを削除）。
- 初期セキュリティ点検：Firestoreルールが `users/{userId}` を本人のみ read/write になっていることを確認（安全・テストモードではない）。
- Phase 0 一式を commit `1666233` で `main` にpush。

**詰まり＆対処（再発時の参考）**
- 初回デプロイで `iam.serviceaccounts.actAs` の403 → IAM伝播待ちの一過性。1〜2分後に再デプロイで成功。
- `secrets:set` を `!`（非対話シェル）で実行 → 失敗。**実ターミナル**で実行する。
- 最初に誤ってプレースホルダ `sk-ant-…`（三点リーダ入り）を登録 → `not a legal HTTP header value`。**フルの正規キーを1行で**登録し直して解決。
- gitのpush認証が未設定（過去はGitHub Web編集）→ classic PAT（repoスコープ）で `git push`。

**決定事項**
- ホスティングは当面 GitHub Pages のまま。非公開化は実害が薄く（リポジトリに秘密は無い）、むしろサイトが落ちるので見送り。やるなら Firebase Hosting 移行後。
- 許可ユーザーは当面2名のみ（課金保護）。

⏭ **次：Phase 1（③添削）** — 既存の草案編集UIに `callAI()` を薄く接続。素材庫・草案に溶け込ませ、"機能のための機能"にしない（設計原則）。

---

## ドキュメント整備　✅ 完了（2026-06-21）
- `CLAUDE.md`（プロジェクト指針）と本 `PROGRESS.md`（進捗ログ）を作成。今後のセッションはまず CLAUDE.md を読んで文脈を引き継ぐ。
